iiiGUI & CL
fast
secure
branches
git init to intializing the empty git
git confih --global user.name "raju"
git config --global user.mail "raju@gmal.com"
git config --list
work space 
staging/index
local repo
...................
touch file
git status
git add
git commit -m "label message" filename
git log
git show commit id
git add.
git add *
git add -A
git reset HEAD filename (staging to workspace)
git reset --soft cid (local to staging)
git reset --mixed cid (local to workspace)
git reset --hard cid (for delete)
